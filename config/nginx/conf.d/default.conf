
upstream appupstream {
    server pywishapp:8000;
    # server srv2.example.com;
    # server srv3.example.com;
}

server {
    listen 80;
    server_name appupstream;

    location / {
        proxy_pass http://appupstream;
    }
}



# http {
#     # first we declare our upstream server, which is our Gunicorn application
#     upstream pywishappus {
#         # docker will automatically resolve this to the correct address
#         # because we use the same name as the service: "djangoapp"
#         server pywishapp:8000;
#     }

#     # now we declare our main server
#     server {

#         listen 88;
#         server_name pywish.local.com;

#         access_log /var/log/nginx/access.log;
#         error_log /var/log/nginx/error.log;

#         location / {
#             # everything is passed to Gunicorn
#             proxy_pass http://pywishappus;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header Host $host;
#             proxy_redirect off;
#         }

#         # location /static/ {
#         #     alias /opt/services/pywishapp/static/;
#         # }

#         # location /media/ {
#         #     alias /opt/services/pywishapp/media/;
#         # }
#     }
# }